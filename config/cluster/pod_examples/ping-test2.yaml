apiVersion: v1
kind: Pod
metadata:
  name: ping-test2
  namespace: default
  labels:
    setera.com/tenant: "tenant-a"
spec:
  containers:
    - name: toolbox
      image: busybox:1.36
      command: ["sleep", "3600"]
      imagePullPolicy: IfNotPresent
  restartPolicy: Always
