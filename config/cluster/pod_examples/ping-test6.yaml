apiVersion: v1
kind: Pod
metadata:
  name: ping-test6
  namespace: default
  annotations:
    setera.com/tenant: "two"
spec:
  containers:
    - name: toolbox
      image: busybox:1.36
      command: ["sleep", "3600"]
      imagePullPolicy: IfNotPresent
  nodeSelector:
    setera.com.tenant.two: "Enabled"
    kubernetes.io/hostname: "setera-cluster-orch-dev-worker"
